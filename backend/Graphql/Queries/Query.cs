using GraphQL.Types;

public class Query : ObjectGraphType
{
    public Query()
    {
        /*
        Field<ListGraphType<UserType>>(
            "users",
            resolve: context =>
            {

            }

            )
        */
    }
}